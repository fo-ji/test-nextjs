import type { FC } from 'react';
import { Layout } from '@/components';

const Home: FC = () => (
  <Layout title="Home">
    <p className="text-4xl">Welcome to Nextjs</p>
  </Layout>
);

export default Home;
